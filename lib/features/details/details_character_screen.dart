import 'package:flutter/material.dart';

class DetailsCharacterScreen extends StatelessWidget {
  const DetailsCharacterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text()),
    );
  }
}
